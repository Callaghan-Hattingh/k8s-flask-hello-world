apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoamiv2-ingress
spec:
  rules:
  - http:
      paths:
      - path: /2
        pathType: Prefix
        backend:
          service:
            name: whoamiv2
            port:
              name: web